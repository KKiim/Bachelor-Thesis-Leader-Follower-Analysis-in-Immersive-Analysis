//NODE_SERVER_PORT has to be the same in Cesium/Sync/web.config

var CONFIG = {
    NODE_SERVER_IP: 'localhost',
    NODE_SERVER_PORT: 8081,
    UDP_PORT: 21567,
    UDP_HOST: 'localhost'
};

module.exports = CONFIG;
